{
  "name": "playlist",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/playlist/src",
  "projectType": "library",
  "tags": ["feat"],
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/playlist/**/*.{ts,tsx,js,jsx}"]
      }
    }
  }
}
